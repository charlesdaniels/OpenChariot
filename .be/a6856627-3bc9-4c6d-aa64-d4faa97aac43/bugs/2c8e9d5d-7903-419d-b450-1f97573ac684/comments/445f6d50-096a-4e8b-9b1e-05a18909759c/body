It would be ideal if a consistent look and feel could be maintained across
all pages generated by OpenChariot. This could be accomplished by writing
a custom CSS theme, and overwriting the CSS of the various tools which
OpenChariot uses for site generation.

